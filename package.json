{
  "name": "tetu-voting-system",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/contracts",
    "packages/web",
    "packages/shared"
  ],
  "scripts": {
    "build": "npm run build -ws --if-present",
    "lint": "npm run lint -ws --if-present",
    "test": "npm run test -ws --if-present",
    "dev:stack": "node scripts/dev-stack.mjs",
    "dev:stack:e2e": "node scripts/dev-stack-e2e.mjs",
    "test:e2e:full": "npm run test:e2e -w packages/web"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.44.1",
    "@typescript-eslint/parser": "^8.44.1",
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "^3.6.2",
    "typescript": "^5.9.2"
  }
}
